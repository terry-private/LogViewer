# ``LogViewer``

ログの記録からSwiftUI画面での確認までを、ひとつのimportで利用する互換製品。

## Overview

`import LogViewer`は`LogViewerCore`と`LogViewerUI`の公開APIを再公開する。
最短の導入例、専用ウインドウ、シェイク、プライバシーの実用例はリポジトリの
READMEを参照する。画面を使わない場合は`LogViewerCore`製品、SwiftLogと接続する
場合は`LogViewerSwiftLog`製品を選択できる。
